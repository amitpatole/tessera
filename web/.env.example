# Backend the UI proxies to (server-side only — never exposed to the browser).
TESSERA_API_URL=http://127.0.0.1:8080
# Required when the backend binds non-loopback (Cloud Run). Leave empty for local loopback.
TESSERA_API_TOKEN=
